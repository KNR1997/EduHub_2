export type StudentInterface = {
    id: number
    firstName: string;
    lastName: string;
    email: string;
    contactNo: string;
    dob: string;
    classroomName: string;
    contactPerson: string;
};

export type ClassInterface = {
    id: number
    name: string;
};